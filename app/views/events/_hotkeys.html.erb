<% content_for :footer do %>
	
	<script type="text/javascript">
		
		<% @classifications.each do |klass| %>
				
			$(document).bind('keydown', '<%= klass.shortcut %>', function() {
				set_classification(<%= klass.id %>);
				return false;
			});
		
		<% end %>
		
	</script>
	
<% end %>
