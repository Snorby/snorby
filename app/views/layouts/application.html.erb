<!DOCTYPE html>

<html>
  <head>
    <meta http-equiv="Content-type" content="text/html; charset=UTF-8" />
		<title><%= content_for?(:title) ? yield(:title) : "Snorby #{Snorby::VERSION}" %></title>

		<meta http-equiv="X-UA-Compatible" content="chrome=1">
    <meta name="description" content="Snorby - All About Simplicity" />
    <meta name="keywords" content="" />
    <link rel="icon" href="/favicon.png" type="image/png" />

    <%= stylesheet_link_tag 'grid', 'snorby', :cache => false %>
		<%= javascript_include_tag 'jquery', 'jquery-ui', 'mustache', 'growl', 'colorpicker', 'scrollto', 'snorby', :cache => false %>
    <%= csrf_meta_tag %>

		<%= yield(:header) %>

		<style type="text/css">
			<%- Severity.all.each do |sev| -%>
				<%= "ul.table li div.small span.severity.sev#{sev.sig_id} {background-color: #{sev.bg_color};color: #{sev.text_color};}" %>
			<%- end -%>
		</style>

  </head>
  
	<body>
		
		<%- flash.each do |type, message| -%>
      <script type="text/javascript" charset="utf-8">
				flash_message.push({type: "<%= type %>", message: "<%= message %>"});
				flash();
      </script>
    <%- end -%>
    
		
		<%= render :partial => 'layouts/header' %>

		<%= render :partial => 'layouts/content' %>

		<%= render :partial => 'layouts/footer' %>
		
		<%= yield(:footer) %>
  </body>

</html>
